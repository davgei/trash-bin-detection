# Core dependencies for the labeling tools and training pipeline.
# Install with: pip install -r requirements.txt

opencv-python       # image display, drawing, and video frame extraction
ultralytics         # YOLO model training and inference
numpy               # array operations (pulled in by opencv and ultralytics)
